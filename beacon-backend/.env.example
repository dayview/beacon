# Server
PORT=3001
NODE_ENV=development

# Database
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/beacon?retryWrites=true&w=majority

# Auth
JWT_SECRET=CHANGE_ME_generate_a_strong_random_secret
JWT_EXPIRE=7d

# Miro OAuth
MIRO_CLIENT_ID=
MIRO_CLIENT_SECRET=
MIRO_REDIRECT_URI=https://yourdomain.com/settings

# AI Keys (platform pooled keys for free tier)
BEACON_OPENAI_KEY=
BEACON_ANTHROPIC_KEY=

# Encryption (32-byte hex key — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=

# CORS
FRONTEND_URL=http://localhost:5173
