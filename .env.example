# Frontend Configuration (Vite)
# Optional in dev — vite.config.ts already proxies /api to the backend on
# localhost:3001, so requests work even if this is left unset. Only needed
# to point at a different backend (e.g. a deployed one) or in production.
VITE_API_URL=http://localhost:3001/api
